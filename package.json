{
  "name": "keeep",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@kmuenster/prettier-config": "^1.0.1",
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "packageManager": "yarn@1.22.19",
  "prettier": "@kmuenster/prettier-config",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
